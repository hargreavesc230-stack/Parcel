# parcel

To install dependencies:

```bash
bun install
```

To run the API:

```bash
bun run dev:api
```

To run checks (server must already be running):

```bash
bun run check
```

Existing endpoint:

- `GET /health` -> `ok`
